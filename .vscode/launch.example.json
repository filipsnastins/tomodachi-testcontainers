{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "pytest",
      "type": "python",
      "request": "test",
      "cwd": "${workspaceFolder}",
      "justMyCode": true
    },
    {
      "name": "Python: attach to debugger (examples/src)",
      "type": "python",
      "request": "attach",
      "port": 5678,
      "host": "localhost",
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}/examples",
          "remoteRoot": "/app"
        }
      ]
    }
  ]
}
